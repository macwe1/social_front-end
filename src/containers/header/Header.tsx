import { Header } from '@components/header/Header'
// import { AppState } from '@store/store'
// import { Dispatch } from 'redux'
import { connect } from 'react-redux'

export const HeaderContainers = connect()(Header)
